import { DashboardPage } from '@/components/dashboard/DashboardPage';

export const metadata = {
  title: 'Dashboard | ProAgent',
  description: 'Panel de control de ProAgent',
};

export default function Dashboard() {
  return <DashboardPage />;
}
